import React from 'react';
import PropTypes from 'prop-types';

const Events = props => {
    return (
        <div>
            <h1>Events</h1>
        </div>
    );
};

Events.propTypes = {
    
};

export default Events;